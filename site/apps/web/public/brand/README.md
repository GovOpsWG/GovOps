# Brand assets

| File                      | Use                                         |
| ------------------------- | ------------------------------------------- |
| `govops-lockup-light.png` | Mark plus white wordmark, for dark surfaces |
| `govops-lockup-dark.png`  | Mark plus navy wordmark, for light surfaces |
| `govops-mark.png`         | Mark alone: favicon and narrow viewports    |

Transparent PNGs derived from the designer's `GovOps Logo Var-3a.png`. A vector lockup is coming;
when it lands, replace these with a single inline SVG in `app/components/logo.tsx` and delete the
light/dark swap it needs.
